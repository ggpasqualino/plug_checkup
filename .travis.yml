language: elixir
sudo: false
elixir: 1.9.4
otp_release: 22.1.4

env:
  - MIX_ENV=test

before_script:
  - mix deps.compile

script:
  - mix compile --warnings-as-errors
  - mix credo
  - mix coveralls.travis
